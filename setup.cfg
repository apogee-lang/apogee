[metadata]
name = apogee-lang
version = 0.1.0
description = Apogee — a programming language for the AI era

[options]
packages = find:
python_requires = >=3.9

[options.entry_points]
console_scripts =
    apogee = src.cli:main
